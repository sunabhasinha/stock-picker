# Application-shell dependencies (ADR-0005). The engine (sunabha_agent/)
# stays stdlib-only per ADR-0002 and never imports these.
pyyaml>=6.0            # the shell imports the engine, which needs it (universe lists)
sqlalchemy>=2.0
alembic>=1.13
psycopg[binary]>=3.1   # Postgres driver; tests run on SQLite and don't need it
fastapi>=0.115         # M2 auth server (ADR-0006)
uvicorn>=0.30
argon2-cffi>=23.1      # password hashing - argon2id (ADR-0006, never hand-rolled)
httpx>=0.27            # FastAPI TestClient transport (tests)
