# Spec: <feature name>
status: proposed | accepted | done (PR #)

Goal: <one or two sentences — the user-visible outcome>

Acceptance:
- <observable, checkable statements — these become tests>

Invariants (inherited from AGENTS.md apply always; add feature-specific):
- <constraints the implementation must not violate>

Out of scope: <explicitly, so the agent doesn't gold-plate>

Deliberately left to the implementer: <named open choices — delegated
judgment, must be recorded when exercised>
NOT left open: <choices that look open but aren't — state them>
